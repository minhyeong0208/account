<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="LoginMapper">
	
	<!-- 로그인 사용자 정보 조회 -->
	<select id="selectLogin" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			USERID
		  , USERNM
		  , PASSWD
		  FROM ACC_USER
		 WHERE USERID = #{USERID}
	</select>
	
	<!-- 아이디 중복체크 -->
	<select id="dupCheckId" parameterType="java.util.Map" resultType="int">
		SELECT
			COUNT(*)
		  FROM ACC_USER
		 WHERE USERID = #{USERID}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertUser" parameterType="java.util.Map">
		INSERT INTO ACC_USER (
			USERID
		  , PASSWD
		  , BIRTHDATE
		  , USERNM
		  , PHONENUM
		  , REGID
		  , EMAIL
		  , REGDATE
		) VALUES (
			#{USERID}
		  , #{PASSWD}
		  , #{BIRTHDATE}
		  , #{USERNM}
		  , #{PHONENUM}
		  , #{USERID}
		  , #{EMAIL}
		  , NOW()
		)
	</insert>
	
	<!-- 아이디 + 이메일로 유저 확인 -->
	<select id="selectUserByIdAndEmail" parameterType="java.util.Map" resultType="int">
	    SELECT COUNT(*)
	    FROM ACC_USER
	    WHERE USERID = #{USERID}
	    AND EMAIL = #{EMAIL}
	</select>
	
	<!-- 비밀번호 업데이트 -->
	<update id="updatePassword" parameterType="java.util.Map">
	    UPDATE ACC_USER
	    SET PASSWD = #{PASSWD}
	    WHERE USERID = #{USERID}
	</update>
</mapper>