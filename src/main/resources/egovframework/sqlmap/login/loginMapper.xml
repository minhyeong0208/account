<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="LoginMapper">
	
	<!-- 로그인 사용자 정보 조회 -->
	<select id="selectLogin" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			USERID
		  , USERNM
		  , PASSWD
		  FROM ACC_USER
		 WHERE USERID = #{USERID}
	</select>
	
	<!-- 아이디 중복체크 -->
	<select id="dupCheckId" parameterType="java.util.Map" resultType="int">
		SELECT
			COUNT(*)
		  FROM ACC_USER
		 WHERE USERID = #{USERID}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertUser" parameterType="java.util.Map">
		INSERT INTO ACC_USER (
			USERID
		  , PASSWD
		  , BIRTHDATE
		  , USERNM
		  , PHONENUM
		  , REGID
		  , EMAIL
		  , REGDATE
		) VALUES (
			#{USERID}
		  , #{PASSWD}
		  , #{BIRTHDATE}
		  , #{USERNM}
		  , #{PHONENUM}
		  , #{USERID}
		  , #{EMAIL}
		  , NOW()
		)
	</insert>
	
</mapper>